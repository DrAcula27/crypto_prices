const express = require("express");

const app = express();

// get requests from front end
// make call to coin api
// get data
// send to front end

app.listen(5000, () => {
  console.log(`Server is Listening on 5000`);
});
